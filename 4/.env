# SQLite lives under /app/data (mounted volume)
DATABASE_URL=sqlite:////app/data/shipyard.db

# MQTT broker host is the docker service name
MQTT_BROKER=mqtt
MQTT_PORT=1883

# CORS (set your production domain later, keep * for demo behind nginx)
CORS_ALLOW_ORIGINS=*
